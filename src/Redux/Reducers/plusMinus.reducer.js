// const initialState = {items: []};

//  export const plusMinusNum = (state = initialState, action) = {
//     switch (action.type) {
//        case  INCREMENT: return {
//         ...state, items : [...state.items, action.payload]
//        };
//        case  DECREMENT: return {
//         ...state, items : [...state.items, action.payload]
//     };
// }
